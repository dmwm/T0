# The version variable assigned here can be used by setup.py file and sphinx. 
# At the same time CMS build system is using SVN tags. Here version value is
# a placeholder which is replaced in t0.spec during RPM build process to get
# correct SVN tag version.
version = 'development' # do not replace it

import { ChapterShell } from "@/components/ChapterShell";

export function SimLifecycle() {
  return (
    <ChapterShell
      eyebrow="Simulator"
      title="Run / Stream / Job lifecycle"
      subtitle="Step a run through each state; see DAOs fire"
      showPrevNext={false}
    >
      <p style={{ color: "var(--muted)" }}>Simulator implementation pending.</p>
    </ChapterShell>
  );
}

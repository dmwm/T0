import { ChapterShell } from "@/components/ChapterShell";

export function Ch6Operator() {
  return (
    <ChapterShell
      eyebrow="Chapter 6"
      title="Operator surface and recap"
      subtitle="bin/t0, replay-by-PR, multi-agent split"
    >
      <h2>What you will learn</h2>
      <p>
        The deploy hosts (vocms047, vocms050xx), the <code>bin/t0</code> CLI,
        the replay-by-PR-comment workflow, and how Main / Helper agents
        partition streams.
      </p>
      <p style={{ color: "var(--muted)" }}>Content port in progress.</p>
    </ChapterShell>
  );
}
